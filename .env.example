# =============================================================================
# Portable Skill Activator - AI Configuration
# by Gabi @ Booplex (booplex.com)
# =============================================================================
#
# The AI only runs when generating INDEX.yaml - not on every message.
# Set your provider below, then configure that provider's section.

# =============================================================================
# STEP 1: Choose Your Provider
# =============================================================================

AI_PROVIDER=claude

# Options:
#   claude     - Uses Claude Code CLI (no API key needed!)
#   openai     - OpenAI API
#   anthropic  - Anthropic API
#   ollama     - Local Ollama (no API key needed)
#   openrouter - OpenRouter (access to many models)
#   custom     - Any OpenAI-compatible endpoint

# =============================================================================
# STEP 2: Configure Your Provider (only uncomment the one you chose)
# =============================================================================

# -----------------------------------------------------------------------------
# CLAUDE (recommended - uses your Claude Code CLI, no API key needed)
# -----------------------------------------------------------------------------
CLAUDE_MODEL=haiku
CLAUDE_FALLBACK_MODELS=sonnet,opus
# Models: haiku (fast), sonnet (balanced), opus (best)

# -----------------------------------------------------------------------------
# OPENAI
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_FALLBACK_MODELS=gpt-4o,gpt-3.5-turbo

# -----------------------------------------------------------------------------
# ANTHROPIC
# -----------------------------------------------------------------------------
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# ANTHROPIC_FALLBACK_MODELS=claude-3-haiku-20240307

# -----------------------------------------------------------------------------
# OLLAMA (local, free - no API key needed)
# -----------------------------------------------------------------------------
# OLLAMA_MODEL=llama3
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_FALLBACK_MODELS=mistral,codellama

# -----------------------------------------------------------------------------
# OPENROUTER (access to many models)
# -----------------------------------------------------------------------------
# OPENROUTER_API_KEY=sk-or-...
# OPENROUTER_MODEL=anthropic/claude-3-haiku
# OPENROUTER_FALLBACK_MODELS=openai/gpt-4o-mini

# -----------------------------------------------------------------------------
# CUSTOM (any OpenAI-compatible API)
# -----------------------------------------------------------------------------
# CUSTOM_API_KEY=your-key
# CUSTOM_MODEL=your-model
# CUSTOM_BASE_URL=https://your-endpoint.com/v1
# CUSTOM_FALLBACK_MODELS=model2,model3

# =============================================================================
# OPTIONAL: Generation Settings (applies to all providers)
# =============================================================================

AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.3
AI_TIMEOUT=60
AI_MAX_RETRIES=3
AI_RETRY_DELAY=2.0
AI_RATE_LIMIT_RPM=20
AI_RATE_LIMIT_DELAY=0.5
AI_LANGUAGES=english
